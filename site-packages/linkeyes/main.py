import linkeyes
import sys

from linkeyes.configpy import FileConfig

if __name__ == "__main__":
    if len(sys.argv) == 1:
        print("Please specify a configuration file.")
    cfg = FileConfig(sys.argv[1])
    linkeyes.postlinks(cfg)
